/target/
/.worktrees/
/.flatpak-builder/
/build/
*.swp
*.swo

# The pinned rustpush checkout is patched in place only for a build. Never
# allow local FairPlay or compatibility material to enter the superproject.
/vendor/rustpush/certs/fairplay/
/compat/
